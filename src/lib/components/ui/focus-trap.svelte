<!-- shields the page while a floating layer is open, so the click that dismisses it doesn't also
	 activate whatever it landed on, and the anchor can't scroll out from under the content -->
<div class="fixed inset-0 z-40 select-none"></div>
